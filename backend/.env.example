# ============================================
# BACKEND ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to '.env' and fill in your values
# NEVER commit .env to git (it should be in .gitignore)
# ============================================

# ============================================
# DATABASE CONFIGURATION (MySQL)
# ============================================
# The hostname where MySQL is running
# Use 'localhost' for local development
DB_HOST=localhost

# MySQL port (default is 3306)
DB_PORT=3306

# MySQL username (default is 'root' for local dev)
DB_USER=webuser

# MySQL password (leave empty if no password set)
# CHANGE THIS IN PRODUCTION!
DB_PASSWORD=strongpassword

# Database name - will be created if it doesn't exist
DB_NAME=projet_web

# ============================================
# JWT CONFIGURATION
# ============================================
# Secret key used to sign JWT tokens
# IMPORTANT: Change this to a long random string in production!
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production-123456

# ============================================
# SERVER CONFIGURATION
# ============================================
# Port the backend server will run on
PORT=3000

# Environment (development or production)
NODE_ENV=development

# ============================================
# CORS CONFIGURATION
# ============================================
# Frontend URL (for CORS - allow frontend to call API)
# Angular default is http://localhost:4200
FRONTEND_URL=http://localhost:4200
