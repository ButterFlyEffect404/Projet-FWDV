/**
 * Environment Configuration - Production
 * 
 * This file is used for production builds.
 * During build, Angular CLI can replace this file with environment.development.ts
 * based on the configuration in angular.json
 */
export const environment = {
    production: true,
    // Backend API URL - change this for production deployment
    apiUrl: 'http://localhost:3000',
};
